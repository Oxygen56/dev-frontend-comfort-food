{
  "name": "the-home-ratio",
  "version": "1.0.0",
  "private": true,
  "description": "Two original entries for the DEV Frontend Challenge: Comfort Food",
  "scripts": {
    "serve": "http-server docs -p 43217 -c-1",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:update-snapshots": "playwright test --update-snapshots",
    "demos:capture": "node scripts/capture-demos.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.54.2",
    "http-server": "^14.1.1"
  }
}
